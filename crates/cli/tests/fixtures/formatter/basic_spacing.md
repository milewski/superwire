```ai
provider  openai   {driver :  "openai" models:["gpt-4o-mini",]}

   output {   result:"ok" }
```
---
```ai
provider openai {
    driver: "openai"
    models: [
        "gpt-4o-mini",
    ]
}

output {
    result: "ok"
}
```
