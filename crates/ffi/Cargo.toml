[package]
name = "ffi"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
engine-core = { package = "engine-ai-core", path = "../core" }
engine-agent = { package = "engine-ai-agent", path = "../agent" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
