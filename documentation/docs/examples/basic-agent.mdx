---
title: Basic agent
description: Run the canonical strongly typed hello workflow.
---

import HelloWorkflow from "/examples/wire/hello.mdx";

<HelloWorkflow />

Runtime values:

```json
{
  "input": {"name": "Ada"},
  "secrets": {"api_key": "replace-at-runtime"}
}
```

Final workflow object:

```json
{
  "message": "Hello Ada."
}
```

HTTP JSON wraps that value as `{ "output": { "message": "Hello Ada." } }`; the CLI prints the inner object directly.

Every complete workflow example in this documentation is imported from a checked fixture under `examples/wire`. See [Quickstart](/quickstart) to execute this one.
