---
title: Providers
description: Declare model backends and bind agents to them with explicit model calls.
---

Providers are named blocks that hold backend settings.

```txt
provider openai {
    driver: "openai"
    api_key: secrets.openai_api_key
    models: ["gpt-4.1-mini", "gpt-4.1"]
}
```

## Provider syntax

```txt
provider <provider_name> {
    <property_name>: <expression>
    ...
}
```

Every provider property is just a key/value expression pair in the DSL. The parser preserves all properties. The default runtime only consumes a known subset for the built-in provider factory.

## Binding an agent to a provider

Agents select a provider by calling the provider name in the `model` property.

```txt
agent summarizer {
    model: openai("gpt-4.1-mini")
    prompt: "Summarize the release notes."
    output: string
}
```

The model name can be passed either positionally or as a named argument:

```txt
model: openai("gpt-4.1-mini")
model: openai(model: "gpt-4.1-mini")
```

## Validation behavior

Validation checks these provider-related conditions:

- provider names must be unique
- an agent `model:` call must reference a declared provider
- if a provider declares `models`, the chosen model must appear in that list
- the `model:` property must be a provider call, not a string literal or object

## Built-in provider settings in the default runtime

### OpenAI driver

```txt
provider openai {
    driver: "openai"
    api_key: secrets.openai_api_key
    base_url: "https://api.openai.com/v1"
    models: ["gpt-4.1-mini"]
}
```

Supported settings:

- `driver`
- `api_key`
- `base_url`
- `api_endpoint`
- `models`

If `api_key` is omitted, the runtime falls back to `OPENAI_API_KEY`.

### Ollama driver

```txt
provider ollama {
    driver: "ollama"
    api_endpoint: "http://localhost:11434"
    models: ["qwen3.5:8b"]
}
```

Supported settings:

- `driver`
- `api_endpoint`
- `base_url`
- `host`
- `models`

## Practical pattern: local first, cloud second

```txt
provider ollama_local {
    driver: "ollama"
    models: ["qwen3.5:8b"]
}

provider openai_cloud {
    driver: "openai"
    api_key: secrets.openai_api_key
    models: ["gpt-4.1-mini"]
}

agent redact_notes {
    model: ollama_local("qwen3.5:8b")
    prompt: "Remove names and email addresses from {{ input.notes }}."
    output: [string]
}

agent synthesize {
    model: openai_cloud("gpt-4.1-mini")
    prompt: "Summarize {{ agent.redact_notes }} into key insights."
    output: {
        insights: [string]
        recommendations: [string]
    }
}
```

<Info>
  Additional provider properties are still useful with custom provider
  factories because the DSL keeps the full object expression intact.
</Info>
