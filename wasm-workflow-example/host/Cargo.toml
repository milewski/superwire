[package]
name = "wasm-workflow-host"
version = "0.1.0"
edition = "2021"

[dependencies]
schemars = "1.2"
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
superwire-core = { path = "../../crates/core" }
