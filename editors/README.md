# Editor Support for AI Engine DSL

This directory contains editor integrations for the AI Engine DSL (`.ai` files).

## TextMate Grammar for JetBrains IDEs

The `textmate/` directory contains a TextMate grammar bundle for syntax highlighting in JetBrains IDEs.

See `textmate/README.md` for installation instructions.
